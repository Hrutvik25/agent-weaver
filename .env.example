# Frontend environment variables
# Copy to .env.local for local dev

# When running via Docker/Nginx, leave blank (requests go to /api relative path)
# When running frontend standalone against a remote backend, set this:
VITE_API_URL=
